
/**
 *  Integer of pixels between the component's left border
 *  and the left edge of its content area.
 *  <p>The default value is 0.</p>
 */
[Style(name="paddingLeft", type="int", format="Length", inherit="no")]

/**
 *  Number of pixels between the component's right border
 *  and the right edge of its content area.
 *  <p>The default value is 0.</p>
 */
[Style(name="paddingRight", type="int", format="Length", inherit="no")]

/**
 *  Integer of pixels between the container's bottom border
 *  and the bottom of its content area.
 *
 *  @default 0
 */
[Style(name="paddingBottom", type="int", format="Length", inherit="no")]

/**
 *  Integer of pixels between the container's top border
 *  and the top of its content area.
 *
 *  @default 0
 */
[Style(name="paddingTop", type="int", format="Length", inherit="no")]